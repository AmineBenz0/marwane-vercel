/**
 * Page Produits (protégée).
 * 
 * Affiche la liste des produits avec toutes les fonctionnalités CRUD.
 */

import ProduitsList from './Produits/ProduitsList';

function Produits() {
  return <ProduitsList />;
}

export default Produits;

