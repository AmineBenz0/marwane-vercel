/**
 * Page Clients (protégée).
 * 
 * Affiche la liste des clients avec toutes les fonctionnalités CRUD.
 */

import ClientsList from './Clients/ClientsList';

function Clients() {
  return <ClientsList />;
}

export default Clients;

